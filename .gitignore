# Recreated on the gateway from skills/ at each boot — do not commit
.claude/skills

# Ephemeral Discord attachment staging on the gateway (never commit)
.bodhi-discord-incoming/

# Session-local context files
bootstrap/ACTIVE_CONTEXT.md
bootstrap/.ACTIVE_CONTEXT_backups/

# Gateway ephemeral resume-clear markers (legacy bootstrap path; live under /data/resume-clear-markers)
bootstrap/.bodhi_resume_clear.*.json

# Claude session directory
.claude/

# Gateway ephemeral resume-clear markers (legacy; now /data/resume-clear-markers)
bootstrap/.bodhi_resume_clear.*.json
